#include <stdio.h>
void main (){
  int i, n, soma=0;
  printf ("Digite a quantidade de números lidos \n");
  scanf ("%d", &n);
  for (i=1;i<=n;i++){
  soma+=i;
  }
  printf("A soma é: %d \n", soma);
}

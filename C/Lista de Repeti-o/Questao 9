#include <stdio.h>
void main(){
  int i, n, maior=0;
  for (i=1;i<=5;i++){
    printf("Digite um número: \n");
    scanf ("%d", &n);
    if (n>maior){
      maior=n;
    }
  }
    printf ("O maior número é: %d", maior);
}
